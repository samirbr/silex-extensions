
                <ul class="unstyled inline">

                {%- for action in record_actions %}

                    <li>
                        <a btn btn-mini" href="{{ "{{ path('"~ route_name_prefix ~"_"~ action ~"', { 'id': entity.id }) }}" }}">{{ action }}</a>
                    </li>

                {%- endfor %}

                </ul>
